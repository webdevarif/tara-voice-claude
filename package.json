{
  "name": "tara-vscode",
  "displayName": "Tara — Voice AI Coding Assistant",
  "description": "Voice-controlled AI coding assistant powered by Gemini Live + Claude Code",
  "version": "0.1.0",
  "publisher": "tara",
  "author": "Tara",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/webdevarif/tara-voice-claude.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "keywords": [
    "voice",
    "ai",
    "claude",
    "gemini",
    "coding assistant"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "icon": "media/tara-icon.png",
  "contributes": {
    "commands": [
      {
        "command": "tara.openChat",
        "title": "Tara: Open Chat Panel",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "tara.startVoice",
        "title": "Tara: Start Voice Input",
        "icon": "$(mic)"
      },
      {
        "command": "tara.stopAgent",
        "title": "Tara: Stop Current Agent",
        "icon": "$(debug-stop)"
      },
      {
        "command": "tara.openKanban",
        "title": "Tara: Open Kanban Board",
        "icon": "$(project)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "tara-sidebar",
          "title": "Tara",
          "icon": "media/tara-sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "tara-sidebar": [
        {
          "type": "webview",
          "id": "tara.chatView",
          "name": "Chat"
        },
        {
          "type": "webview",
          "id": "tara.kanbanView",
          "name": "Kanban"
        }
      ]
    },
    "configuration": {
      "title": "Tara",
      "properties": {
        "tara.geminiApiKey": {
          "type": "string",
          "default": "",
          "markdownDeprecationMessage": "Deprecated: the key now lives in VS Code's encrypted SecretStorage. Any value here is moved there on first use and then cleared. Enter the key on Tara's setup screen instead.",
          "description": "Legacy plaintext Gemini API key. Migrated to SecretStorage automatically."
        },
        "tara.claudeCodePath": {
          "type": "string",
          "default": "claude",
          "scope": "machine",
          "description": "Path to the claude CLI binary (defaults to 'claude' on PATH). Machine-scoped so a repository's .vscode/settings.json cannot point Tara at a different binary."
        },
        "tara.ffmpegPath": {
          "type": "string",
          "default": "ffmpeg",
          "scope": "machine",
          "markdownDescription": "Path to the `ffmpeg` binary used as the **fallback** microphone recorder. Tara normally records through a bundled native module and needs no external tool; ffmpeg is only used where that module has no prebuilt binary. Either way capture runs in the extension host, because VS Code's webview iframes are created without `microphone` in their Permissions-Policy allow list, so `getUserMedia` cannot work there ([vscode#250568](https://github.com/microsoft/vscode/issues/250568)). Machine-scoped so a repository cannot point Tara at a different binary.",
          "description": "Path to the ffmpeg binary used as the fallback microphone recorder."
        },
        "tara.model": {
          "type": "string",
          "default": "",
          "description": "Model for Claude Code to use (e.g. claude-opus-5). Leave empty to use the CLI's own default."
        },
        "tara.permissionMode": {
          "type": "string",
          "enum": [
            "acceptEdits",
            "auto",
            "manual",
            "dontAsk",
            "plan",
            "bypassPermissions"
          ],
          "enumDescriptions": [
            "Apply file edits without asking, but still gate other tools.",
            "Let Claude Code decide per tool.",
            "Ask before every tool use.",
            "Never ask; skip anything that would need approval.",
            "Plan only — make no changes.",
            "Skip every permission check. Use only in a throwaway workspace."
          ],
          "default": "acceptEdits",
          "scope": "machine",
          "description": "Permission mode passed to Claude Code. This is the real guardrail on what agents may do. Machine-scoped so a repository cannot lower it for you."
        },
        "tara.maxTurns": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "description": "Maximum agent turns per task before Claude Code stops."
        },
        "tara.maxConcurrentAgents": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "description": "Maximum number of Claude Code agents running at once."
        },
        "tara.replyTimeoutSeconds": {
          "type": "number",
          "default": 300,
          "minimum": 30,
          "description": "How long an agent waits for your answer to a clarifying question before it closes."
        },
        "tara.geminiLiveModel": {
          "type": "string",
          "default": "gemini-3.1-flash-live-preview",
          "description": "Gemini Live model for speech-to-text and text-to-speech."
        },
        "tara.language": {
          "type": "string",
          "default": "auto",
          "markdownDescription": "Which language Tara listens for and replies in — a BCP-47 code such as `bn-IN`, `en-US` or `hi-IN`, or `auto` to follow whatever you speak. Set it explicitly if you have a strong accent: left on `auto` the model has to guess the language, and a wrong guess is what turns accented speech into garbled English. Applied through the session's system instruction rather than `speechConfig.languageCode`, because native-audio Live models do not accept a language code.",
          "description": "Language Tara listens for and replies in (BCP-47 code, or 'auto' to follow the speaker)."
        },
        "tara.voiceName": {
          "type": "string",
          "default": "Aoede",
          "description": "Prebuilt Gemini voice used when Tara speaks (e.g. Aoede, Kore, Puck, Charon)."
        },
        "tara.speakResponses": {
          "type": "boolean",
          "default": true,
          "description": "Speak agent answers, clarifying questions and warnings aloud."
        },
        "tara.voiceMode": {
          "type": "string",
          "enum": [
            "hands-free",
            "disabled"
          ],
          "enumDescriptions": [
            "Click the orb to listen continuously. Audio is only sent while you are actually speaking; after 15s of silence Tara sleeps until it hears \"wake up\".",
            "No voice input at all."
          ],
          "default": "hands-free",
          "description": "Voice input mode."
        },
        "tara.riskConfirmation": {
          "type": "boolean",
          "default": true,
          "scope": "machine",
          "description": "Ask for confirmation before running a command that looks destructive (rm -rf, force push, DROP TABLE). Machine-scoped so a repository cannot switch the gate off."
        }
      }
    },
    "keybindings": [
      {
        "command": "tara.startVoice",
        "key": "ctrl+shift+space",
        "mac": "cmd+shift+space",
        "when": "tara.chatViewFocused"
      },
      {
        "command": "tara.stopAgent",
        "key": "ctrl+shift+.",
        "mac": "cmd+shift+.",
        "when": "tara.agentRunning"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "npm run build:extension && npm run build:webview",
    "build:extension": "tsc -p ./tsconfig.json",
    "build:webview": "cd webview-ui && npm run build",
    "watch": "tsc -watch -p ./tsconfig.json",
    "watch:webview": "cd webview-ui && npm run dev",
    "compile": "tsc -p ./tsconfig.json",
    "lint": "eslint src --ext ts",
    "package": "vsce package",
    "install:all": "npm install && cd webview-ui && npm install"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.10",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^2.24.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@picovoice/pvrecorder-node": "^1.2.9",
    "@picovoice/pvspeaker-node": "^1.0.3",
    "ws": "^8.17.0"
  }
}
